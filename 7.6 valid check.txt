입력 유효성 검사(이상한데이터입력하는거)
	1)jsp에서(server측에서) -try, if
		모든 사용자들의 데이터를 서버에서 다 검사?

	2)html에서(사용자 측에서) - ?
		자기가 똑바로 썼는지 체크 -> 잘못썼으면 jsp로 요청 출발도 안하게
		서버측에 부담이 가지 않게
		
HTML, CSS - 체크 -x
JavaScript 사용자측에서 실행되는 프로그래밍 언어
